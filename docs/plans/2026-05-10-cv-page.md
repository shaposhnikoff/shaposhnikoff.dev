# CV Page Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Refresh `/cv/` with the supplied CV content in the existing site style and add browser print-to-PDF support.

**Architecture:** Keep content in Hugo Markdown with small inline HTML blocks for structured CV sections. Extend the existing single stylesheet with CV-specific classes and print media rules; no custom Hugo layout is needed.

**Tech Stack:** Hugo, Markdown/Goldmark unsafe HTML, hand-written CSS.

---

### Task 1: Update CV Content

**Files:**
- Modify: `content/cv/_index.md`

**Steps:**
1. Replace placeholder CV text with the supplied real CV.
2. Use existing `cv-row` and `skills` conventions where they fit.
3. Add semantic class names for contact metadata, highlight metrics, project list, and compact experience details.
4. Keep all visible professional content in English.

**Verification:**
- Run `hugo`.
- Expected: site builds without Markdown or template errors.

### Task 2: Add CV Screen And Print Styles

**Files:**
- Modify: `static/css/main.css`

**Steps:**
1. Add CV-specific classes near the existing CV styles.
2. Preserve the site's restrained palette, monospace labels, thin rules, and compact spacing.
3. Add `@media print` rules to hide site chrome, show link targets, compact spacing, and avoid breaking CV rows across pages.

**Verification:**
- Run `hugo`.
- Expected: generated `public/cv/index.html` contains the refreshed CV.

### Task 3: Final Review

**Files:**
- Inspect: `public/cv/index.html`

**Steps:**
1. Confirm key CV sections render: summary, highlights, skills, experience, projects, interests.
2. Confirm contact links are present.
3. Check git diff for unrelated changes.

**Verification:**
- Run `git diff -- content/cv/_index.md static/css/main.css docs/plans/2026-05-10-cv-page-design.md docs/plans/2026-05-10-cv-page.md`.
